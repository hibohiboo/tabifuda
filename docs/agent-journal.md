# エージェント失敗ジャーナル

形式: 日付 | 何を指示され/意図し、何を誤ったか | 対処(済なら参照先)

- 2026-07-12 | プロジェクト準備の計画立案で、横断的関心事(権限・ログ・セキュリティ・削除方針)とADRを準備工程に含めず、ユーザー指摘で発覚。傾向: ユーザーが明示要求しない限り話題化しない事項が抜けやすい | 済: cross-cutting.md / ADR 0001 作成、スキルにStep 7,8追加
- 2026-07-12 | 上記の失敗をその場で修正したことを理由に、ジャーナルへの記録を省略。「修正済み=記録不要」と誤判断(記録の目的は個別修正ではなく週次の傾向分析)。ユーザー指摘で発覚 | 済: 本ファイル作成。棚卸し時の観点候補: 同系統の「暗黙の必須事項」の抜け(国際化、a11y、バックアップ等)がないか
- 2026-07-12 | phase0-task.mdタスク4でCI(.github/workflows/ci.yml)を実装した際、(1)GitHub Actionsのバージョンをトレーニング知識ベースの記憶で書き、実際の最新版(actions/checkout@v4のはずがv7、gitleaks-action@v2のはずがv3)を確認しなかった、(2)CLAUDE.md最重要ルール1(設計文書が正、実装が従)に反し、ci.ymlの具体的なジョブ構成を裏付ける設計文書を作らずに実装した。ユーザー指摘で発覚 | 済: WebFetchで各actionsのreleasesページを確認して修正、docs/adr/0003-ci-pipeline.mdを追加。傾向: 外部ツールのバージョンは学習知識ではなく都度webで確認する/実装対象が「仕様」か微妙な場合でも設計文書との対応を先に作る
- 2026-07-18 | P1 C1(型骨格実装)で、domain-model.md v0.2がEffect::DealCard/ModifyStatの`Target`型とSetFlag/FlagIsの`FlagValue`型を名前だけ参照し実体を定義していないことに、実装に着手してから気づいた。傾向: 「型骨格を実装」系タスクは着手前に対象文書を通読するだけでなく、登場する型名を全て洗い出し「参照はあるが定義が無いもの」を先に棚卸しすべきだった | 済: 実装前にユーザーに確認(Target=Character(CharacterId)のみ、flags自体をCardKind::Markerカードへ統一し廃止)。domain-model.mdに反映。再発防止をphase-cycleスキル「開始の儀式」手順5に反映(2026-07-18)
- 2026-07-18 | 同C1で、Scenario/SessionのようにVec/HashMapを複数階層ネストする型へ無条件に`#[derive(proptest_derive::Arbitrary)]`を適用し、フィールド単位のコレクションサイズ上限を設けなかったため、roundtripテスト1回の実行が約26分・メモリ数GBに膨張した。自分のバックグラウンド実行は`tail`が最後に一括出力する構成だったため経過が見えず、ユーザーが手元の実行結果を貼ってくれるまで異常に気づけなかった | 済: 各Vec/HashMapフィールドに`#[proptest(strategy = "proptest::collection::vec(..., 0..=3)")]`等でサイズ上限を設定し実行時間を約1秒に短縮。傾向: ネストしたコレクションを複数階層持つ型にArbitrary deriveする際は、フルテストを流す前に最初からサイズ上限を設計する。再発防止をtest-strategy.md §1b・core-invariantsスキル・agent-operations.md(長時間コマンドの可視化)に反映(2026-07-18)
- 2026-07-19 | P1 C2(decide/applyの基本コマンド)着手時、domain-model.mdのCommand/Eventの型骨格を実装前にたどったところ、(1)`decide(state: &Session, ...)`がStartSession(セッション未生成時点で呼ぶ唯一のコマンド)を扱えない、(2)`Command::EndSession`の権限が権限規則表に無い、(3)`Event::CardDealt`にCardInstanceId発行用フィールドが無くcoreがIO/乱数無しに決定的発行できない、の3点で文書が内部的に未確定だった。傾向: 「decide/applyを実装する」系タスクは、Command一覧を実装する前に各CommandのIN(誰が呼べるか)/OUT(どの値をどう生成するか)を1つずつ具体的にたどると、C1の「参照はあるが定義が無い型」と同種の穴(今回は型ではなく状態遷移・権限・ID発行元)が着手前に見つかる | 済: 3点とも実装前にユーザーに確認しdomain-model.md「stateがOptionになる理由」「C2: decide/applyの解決規則」節に反映してから実装。phase-cycleスキル「開始の儀式」手順4に「型だけでなく状態遷移・権限・ID発行元の未確定も対象」と補足する余地あり(未反映)
- 2026-07-19 | P1 C3(権限と提案フロー)着手時、`ProposalId`の発番方法がdomain-model.mdに未確定だった。`CardInstanceId`は「現在のhands+table総数」からの連番方式だが、そのコメントが自ら「除去を導入する将来サイクルでは要再検討」と予告しており、`Propose`→`JudgeProposal`裁定後に`pending_proposal`が`None`に戻る=まさにその「除去」に該当するため同方式は使えなかった(現在状態から過去の提案総数を逆算できない)。傾向: C2の教訓(IN/OUTを具体的にたどるとID発行元の穴が見つかる)は活きたが、既存コードの「将来再検討」という自己予告コメントを次サイクル開始時にまず確認する、という手順にはまだなっていない | 済: 実装前にユーザーに確認(UUIDは決定性違反のため不採用と説明した上で、`Session.proposal_seq: u64`を追加する連番方式を採用)。domain-model.mdの「C3: decide/applyの解決規則」節に反映してから実装。次に同種コメント(「〜の際は要再検討」)を見つけたら、該当サイクル着手時に真っ先に検索する運用が有効か検討の余地あり(未反映)
- 2026-07-19 | P1 C4(シナリオパッチ)着手時、phase1-task.mdのテスト要件「validateの受理/拒否対(現在シーン削除・配布済カード定義消失の検出)」のうち、「現在シーン削除」がdomain-model.md確定済みのPatchOp5種(AddCardDef/ReplaceScene/AddScene/AddTransition/DealCard、すべて追加または既存idへの置換のみ)では原理的に再現不能だと着手前に気づいた(削除系操作が無く、ReplaceSceneもid一致が前提のため既存idが消えることがない)。傾向: タスク文書のテスト要件は「その型・操作セットで実際に到達可能な失敗モードか」を、実装前に一つずつ機械的にトレースする(C1の型棚卸し・C2/C3のIN/OUT/ID発行元トレースと同系統だが、今回は「テスト要件が対象とする失敗モードの到達可能性」という新しい軸) | 済: 実装前にユーザーに確認(受理側の防御的チェックのみ実装し、拒否テストはPatchOp拡充(v1、RemoveScene等)まで保留と合意)。domain-model.mdの「C4: decide/applyの解決規則」節と`patch.rs`のコメントに理由を明記
- 2026-07-19 | P1 C5(プロパティテストによる不変条件の固定)着手時、phase1-task.mdのC5節が「不変条件1〜5」と明記する一方、test-strategy.md自身の「フェーズ別の導入順」表は「不変条件5(パッチ安全性)はP2」としており文書間で矛盾していた。C4でPatchOp/validateが既にP1スコープ内で実装済みだったため技術的には5つとも今できる状態だったが、これはC4完了(この直前のサイクル)によって前提が変わった結果であり、test-strategy.mdの導入順表が更新されずに古いまま残っていたのが原因。傾向: 複数文書にまたがる「フェーズ対応表」は、前サイクルでスコープが前倒しになった時に更新が追いつきやすく漏れる。あるサイクルの成果(ここではC4でのPatchOp前倒し実装)が別文書の前提を変えていないか、次サイクル開始時に確認する視点が要る | 済: 実装前にユーザーに確認(5つ全てをC5で実装する方針に決定)。test-strategy.mdの表とcore-invariantsスキルの該当行を更新してから実装
- 2026-07-19 | 同C5で、不変条件5(パッチ安全性)のプロパティテストを`any::<Session>()`/`any::<ScenarioPatch>()`(完全ランダムなid文字列)でそのまま書きかけたが、SessionとScenarioPatchのid群が互いに独立な乱数文字列になるため、`validate`が「現在シーンが存在しない」でほぼ常に拒否し、検証したい受理側の分岐がほとんど実行されない(見かけ上は緑だが実質何も検証していない)ことに気づいた。傾向: ランダム生成する複数の値が互いに参照し合う関係にある(id同士が一致する必要がある等)場合、独立な`Arbitrary`derive任せでは分岐の踏破率が実質ゼロになりうる。プロパティテストを書いたら「意図した分岐(特に受理側)が本当に一定割合踏まれているか」を実測で確認する習慣が要る | 済: id空間を小さな固定プール(3値)に絞った専用strategyに書き換え、受理側の踏破率を実測(256ケース中61ケース≈24%)して確認してから最終化
- 2026-07-19 | P2 C3(CLIプレイループ)実装時、Dialogueカードの自由入力プロンプトで空Enter(スキップ)を「内側Noneならcontinue」と書いてしまい、本来「本文なしでカードを出す」べきところカードの使用自体をキャンセルする実装をいったん書いた。傾向: 「EOF/スキップ/エラー」を1つの`Option<Option<T>>`に畳み込むヘルパーを書くとき、外側と内側のNoneの意味(終了 vs 本文なし)を呼び出し側で取り違えやすい。手動プレイ(標準入力をpipeで流し込む通しプレイ)で気づいた | 済: 内側Noneはfree_text=Noneとして処理を継続する形に修正(play.rs::run内)。C3はCLIスモークテストがC4スコープのため自動テストでは未固定。C4でCLI結合テストを書く際、この「スキップしても行動は継続する」経路を回帰確認の対象に含めるとよい
- 2026-07-19 | C3のProposalId発番方式の再検討(ADR化前提)を求められた際、decision-logスキルの運用(議論用文書を作成し1論点ずつ進める)に乗せず、論点整理の直後にAskUserQuestionで即決を迫った。傾向: 「設計上の選択肢を提示して人間の回答を待つ」場面はスキル一覧に該当スキルがあるのに、分析→質問の流れを優先してスキル起動の判断が抜けた。ついでに、C3当時の自分の説明「UUIDは決定性違反のため不採用」も外部発行なら純粋性に抵触しない点で不正確だったことが今回の再検討で判明 | 済: docs/tasks/plans/proposal-id-issuance-decisions.md を作成して議論を開始。根拠記述の訂正は同ログの反映先に含めた
- 2026-07-19 | P2 C1/C2(シナリオ形式・lint・テンプレシナリオ)着手時にdocs/design/cross-cutting.mdを読まずに実装した。cross-cutting.md「自由入力(UGC)の取り扱い」§3は作者データ(カード名・text・narration・title・author)へのBoundedString適用をP2(テンプレシナリオ+シナリオlint登場時)に定めていたが、C1/C2ではCardDef/SceneDef/ScenarioMetaを素のStringのまま実装・データ投入してしまい、C3の開始の儀式でcross-cutting.mdを読み直した際に発覚した。傾向: CLAUDE.md「必読文書」表は「横断方針に触れる→cross-cutting.md」だが、C1/C2はシナリオ形式・データ作成という切り口で読み、「作者データの自由入力」という横断的UGC観点だとは判断しなかった。型骨格・データ形式を新設するタスクは、対象が直接「権限・ログ・UGC・削除」を名乗っていなくても、自由入力を含みうるフィールド(名前・説明文・narration等)を持つならcross-cutting.mdも読む、という判断基準が必要 | 済: 着手前にユーザーへ差分を報告し、C3前の小さな修正サイクルとして今すぐ適用する方針の承認を得た上でCardDef.name/text・SceneDef.narration・ScenarioMeta.title/authorをBoundedString化(上限は識別的テキスト200文字・説明的テキスト2000文字)。domain-model.mdに決定を記録
- 2026-07-20 | フォーク出力の設計文書化で、`meta.id`の発番を「既存idと衝突する場合は連番を付す」と書いたが、CLIは手元のシナリオファイル1つしか知らずid空間の全体を照会できないため、この衝突判定は実装不能だった。実装に入る段で気づき「出力ファイル名の語幹と揃える(ファイル衝突回避と発番を共有)」に文書を修正してから実装した。傾向: 設計文書に検証・発番の規則を書くとき、「その判定に必要な情報を、実装主体がその時点で本当に持っているか」を書きながら確認する(C2/C3のID発行元トレースと同系統だが、今回はコアではなくCLI層の文書化で再発) | 済: domain-model.md「フォーク出力」を修正
- 2026-07-20 | docs/tasks再編のコミットをmasterへ直接入れてしまった。前ターンとの間にユーザーがPR #1をマージしローカルをmasterへ切り替えており、コミット前に現在ブランチを確認しなかった(直前まで作業ブランチrdra-viewer-c1にいたという思い込み)。傾向: セッションを跨いで(ユーザーの応答を挟んで)git操作を再開するとき、ブランチ・作業ツリーの状態が前ターンのままだと仮定しがち。コミット・push等の状態変更systemコマンドの直前に git branch --show-current を挟む習慣が要る | 済: ユーザーが「今回はmaster更新でよい」と裁定(ユーザー自身のブランチ切替の戻し忘れも一因)
- 2026-07-20 | docs/tasks再編でコード内コメントのタスク参照6箇所を新パスへ機械的に追従させ、「コード→工程文書という参照が存在すること自体がsmell」だとは気づかなかった(ユーザー指摘で発覚)。傾向: リネーム・移動の追従作業では「壊れた参照を直す」に意識が寄り、「その参照は本来在るべきか(依存の向きは正しいか)」を問う視点が抜ける。参照更新の対象にコードが混ざったら依存の向きを疑うべきだった | 済: CLAUDE.md Rust規約に「コードコメントからdocs/tasks/を参照しない(規範design/のみ可)」を追加し、6箇所からタスク参照を除去。patch_testsの保留決定はtest-strategy.md不変条件5の注記へ移設
- 2026-07-20 | D1のビルド時検証ゲートの動作確認で、sedでtask.mdに意図的な不整合を注入→git checkoutで復元したが、そのファイルには未ステージのfrontmatter追加が乗っており、checkoutがindex状態(frontmatter無し)まで巻き戻して作業内容を消した。傾向: 「一時的に壊して戻す」検証では、復元手段がgit系(checkout/restore)だと未コミットの他の変更も一緒に消える。注入前にコミットしておくか、復元はsedの逆適用など変更範囲を限定した手段にする | 済: frontmatterを再付与しビルド緑を確認
- 2026-07-31 | CI失敗(gen-test-report.mjsの「Running見出しが無い」)を、CIログを確認せずに「cargo本体とテストバイナリの出力タイミング競合」と診断して文書化した。実際はdtolnay/rust-toolchainが設定するCARGO_TERM_COLOR=alwaysでRunning見出しにANSI色が付き行頭アンカーの正規表現が不マッチになる決定的な環境差だった(gh run view --logのステップenv表示で発覚、ローカルで同変数を設定し同一エラーを再現)。傾向: 「ローカルで通りCIで落ちる」問題で、実ログ・実envを見る前に理論(タイミングの揺らぎ)で説明を組み立てた。同一箇所で決定的に再現する失敗は競合ではなく環境差をまず疑い、gh run view --logで実物を確認する | 済: spawnSyncのenvにCARGO_TERM_COLOR=neverを明示し、CI相当環境でのローカル再現が解消することを検証。調査メモ(plans/test-report-running-header-race.md)を訂正
- 2026-08-01 | P3 C1(tabifuda-wasm境界)実装で、wasm-boundary.mdドラフトのdecide関数実装中、actor(UserId、実体は生Stringのnewtype)をJSON文字列としてdecode(&format!("{actor:?}"), ...)する誤った実装を一瞬書いた。IDのnewtypeは`pub struct X(pub String)`だが、JSON経由の値とRustプリミティブ引数を混同し、後者にも一律decodeを適用しようとした。傾向: 「境界を越える値は全部JSONでデコードする」という設計方針を機械的に適用すると、実際はJSON化されていない単純な引数(wasm_bindgenの素の`String`パラメータ)にまで誤って適用しうる | 済: `UserId(actor)`への直接構築に修正。ビルド前のセルフレビューで発覚
- 2026-08-01 | 同C1で、wasm-boundary.mdに明記した方針(console_error_panic_hookの導入、WasmErrorはSerializeのみでDeserialize不要)を、実装(lib.rs)に反映し忘れたままテストを通していた。design-syncで発覚。傾向: 設計ドラフト→Opusレビュー→文書確定、の後に実装するフローで、文書の「方針」節が実装チェックリストとして機能しておらず、実装完了の確認は「テストが通ること」で止まり文書の全項目を逆引きしていなかった | 済: console_error_panic_hookを`#[wasm_bindgen(start)]`から呼ぶ実装を追加、WasmErrorからDeserializeを削除。今後、文書確定後に実装する際は完了時に文書の各項目を1つずつ実装箇所と突き合わせる(design-syncの「チェック範囲を決める」を実装直後にも軽く回すとよい)
- 2026-08-01 | 「phase3にすすんで」の指示でphase-cycleスキルを起動した際、`git branch -a`で既存ブランチを確認せず作業を始めた。実際には過去のセッションで作成された空の`phase3`ブランチ(Phase2完了時点のマーカー、コミット無し)が既に存在しており、それとは別に(ユーザー用意の)`p3`ブランチで作業を進めてしまい、同じフェーズに対して命名違いの2ブランチが並立する状態を後から人間指摘で発見した。マージ済みブランチ(docs-site-*・phase0〜2・rdra-viewer-c1)が9本放置されていたのも同時に発覚。傾向: ブランチ運用ルール(2026-08-01追加)を書いた直後にもかかわらず、「既存のphaseNブランチの有無を確認する」手順も「マージ後に作業ブランチを削除する」手順も最初のバージョンには入れておらず、規約を書いた自分自身が抜けに気づいていなかった | 済: 空のphase3ブランチを削除しp3をphase3へリネーム、マージ済み9ブランチを削除。phase-cycleスキル「0. 開始の儀式」に既存phaseNブランチの確認、「4. 終わり方」にマージ後の削除手順を追記
- 2026-08-01 | P3 C1で`.github/workflows/ci.yml`の`wasm-test`ジョブに`TS_RS_EXPORT_DIR`環境変数を設定せずコミットしていた。ローカル検証時は毎回手で環境変数を指定していたため気づかず、`cargo test -p tabifuda-core --features ts export_bindings`がts-rsの既定出力先(`crates/tabifuda-core/bindings/`)へ書き出し、CIのドリフト検査対象(`crates/tabifuda-wasm/bindings/`)とは別の場所になるため、tabifuda-core側の型変更が実質検証されないまま緑になるバグを作っていた。ユーザーからREADME作成を頼まれ「更新は自動か」と聞かれて再検証した際に発覚。傾向: ローカルでのみ環境変数を都度指定して動作確認すると、その環境変数をCI設定に反映し忘れても手元は正しく動くため気づけない。生成コマンドをCIやドキュメントに書き写す際は、ローカルで使ったコマンド文字列をコピーではなく再入力すると、暗黙の前提(環境変数等)が抜け落ちやすい | 済: ci.ymlに`env: TS_RS_EXPORT_DIR`を追加、修正後のコマンドをローカルで再現し正しい場所にのみ生成されることを確認。crates/tabifuda-wasm/README.mdとADR 0003にも同じ注意書きを追記
- 2026-08-01 | P3 C2(apps/web骨格)で、新規apps/webのtypescriptバージョンをtools/docs-siteに合わせて`~7.0.2`のまま`pnpm add`したところ、`typescript-eslint@8.65.0`がTypeScript 7.0系に未対応でESLint実行時にエラー終了した。`pnpm add`時点やtypecheck実行時点ではエラーが出ず、lintを実行して初めて発覚した。傾向: 兄弟パッケージ(docs-site)が使っているバージョンをそのまま流用すると、そのツールチェーン全体(ここではlinter)の対応状況までは検証されない。バージョンを合わせて追加した後は、typecheck/build/lintの全コマンドを実際に走らせて初めて非互換が判明することがある | 済: apps/webのtypescriptのみ`6.0.3`(typescript-eslintが対応する最新の6系)に固定。パッケージごとに独立したバージョンを許容するpnpm workspaceの前提どおりのため設計文書の更新は不要と判断
- 2026-08-01 | 同C2で、一時検証スクリプト(apps/web/verify.mjs)と screenshot を片付ける`rm -f`コマンドを、直前の`cd apps/web && node verify.mjs`でBashツールの永続cwdがapps/webに変わったままの状態で`rm -f apps/web/verify.mjs ...`(リポジトリルート基準の相対パス)として実行してしまい、実際には存在しない`apps/web/apps/web/verify.mjs`を指して`-f`がエラーを黙って握りつぶし、削除が起きていなかった。直後の`git status`で未削除に気づいた。傾向: Bashツールはコマンド間でcwdが持続するため、直前のコマンドでcdした後に「リポジトリルート基準のつもりの相対パス」を使うと、`-f`等のエラー抑制フラグ付きコマンドは失敗を隠したまま何もしない。cwdを変えた直後の相対パスコマンドは、実行前に`pwd`で確認するか絶対パス/明示的な`cd`を使う | 済: `cd`でリポジトリルートに戻してから`rm`を再実行し削除を確認
- 2026-08-01 | 同C2の完了報告で、`typescript-eslint`のTS 7.0系未対応を検出した際に自分の判断だけで`apps/web`の`typescript`を`6.0.3`に固定し`pnpm add`まで実行したにもかかわらず、最終報告で他の(実際にユーザーへ確認した)判断と一括りにして「いずれもユーザーに確認済み」と書いてしまった。ユーザー指摘で発覚(実際には確認していないとの指摘)。傾向: 実装中に単独で下した技術判断と、その場でユーザーに確認した判断を、セッション終わりの報告でまとめて書くと区別が曖昧になり「確認済み」と誤って一般化しやすい。報告時は判断ごとに「その場で確認したか/自分の裁量で決めて事後報告か」を明示的に分けて書く | 済: docs/adr/0006-frontend-framework.mdに7系へ上げる条件(typescript-eslintのTS7.0対応待ち)を追記し、ユーザーへ事後承認を得た
- 2026-08-01 | P3 C3(冒険記タイムラインUI)で、GM裁定UIの「カードを配って応える」機能が作る`CardDef.text`(回答文)の入力上限に、実行時自由入力用の`FREE_TEXT_MAX`(4096、`PlayCard.free_text`/`Propose.text`/`ScenarioPatch.note`用)を流用してしまった。domain-model.md「文字列の長さ上限」は`CardDef.text`を作者データ(説明的な長いテキスト)の上限である2000文字と明記しており、4096は誤り。サイクル終わりのdesign-syncチェックで気づいた(実装中には気づかず)。傾向: 既存の「自由入力用定数」が1つだけ手元にあると、新しいテキスト入力欄を作るときに用途(実行時自由入力か作者データか)を確認せずそれを使い回しがちになる。UIに文字数上限を設ける際は、その値が最終的にどの型のどのフィールドに入るか(BoundedStringの実際の上限)をdomain-model.mdで都度確認する | 済: `session/limits.ts`に`CARD_NAME_MAX`(200)/`CARD_TEXT_MAX`(2000)を追加し、`GmJudgePanel.tsx`をこちらに差し替えた
- 2026-08-01 | P3.5 C1(セッションの保存と再開)着手前に決めたHashMap→BTreeMap化の決定を、決定ログ(wasm-boundary-decisions.md)とdomain-model.mdには反映したが、その論点の背景説明を最初に書いた規範文書docs/design/wasm-boundary.md「既知の課題(P3.5より前に判断)」節の更新を失念しかけ、サイクル終わりのdesign-syncチェックで気づいた(実装中には気づかず)。傾向: 1つの論点が複数文書(決定ログ・規範文書の複数箇所)に分散して記録されている場合、決定後にどこか1箇所だけ直して「反映済み」と思い込みやすい。決定を反映する際は、その論点名でリポジトリ全体をgrepし、言及箇所を全て洗い出してから反映完了とする | 済: wasm-boundary.mdの該当節を解決済みに更新
- 2026-08-02 | P3.5 C3でEvent::RewardsGranted/CardsDiscardedを追加した際、CardRemoved追加時の既存方針(新Event種別はchronicle.rs/oplog.rsのmatchにワイルドカードへ流さず明示アームを足す。domain-model.md P2申し送り参照)を適用し忘れ、両ファイルとも`_`アームに落ちたままC3のdesign-syncを通過した。C3チェックリストには「chronicle.rsの未対応をC4への申し送りとして記録」と書いたが、C4の着手時チェックでこの申し送りを拾わずに終わり方まで進み、最終的にはフェーズ完了ふりかえり(retrospectiveエージェントへのドラフト依頼)で他の観点から機械的に気づかれた。傾向: (1)新しいEvent追加時、「ワイルドカード(`_ =>`)を持つ既存のmatch文がリポジトリ内に他にないか」をgrepする手順がdesign-syncチェックリストに無く、型レベルの整合(non_exhaustiveのコンパイルエラー)に頼れない箇所は見落とされる。(2)チェックリスト内の「次サイクルへの申し送り」メモは、次サイクルのチェックリスト作成時に自動的には転記されず、機械的に拾われない。前者はdesign-syncのチェック観点(Event/Command/PatchOpのバリアント一覧の過不足)に「`_ =>`ワイルドカードでCLI層が新バリアントを黙って握りつぶしていないか」を明示的に加える余地がある | 済: chronicle.rs(冒険記への明示描画)・oplog.rs(種別名の記録)を追加、テストで固定
- 2026-08-02 | docs-site D5(ワイヤーフレーム)実装で、シナリオ終了後画面のlayoutに「戻る/もう一度遊ぶボタン」を追加したが、apps/webの現行実装にも(inline表示のみでボタン自体が無い)future-requirements.mdにもこの機能の記述が無く、根拠のない憶測だった。同じlayout内の他要素(結果表示は実装済みapps/web、フォーク保存ボタンはfuture-requirements.md§7に明記)は根拠があったため、それらと同じ調子で並べたことで気づかず混入した。D3〜D5の終わり方チェックで「大きな誤解なし」と自己判定した直後にユーザー指摘で発覚。傾向: ワイヤーフレームのように「あると自然に見える」UI要素を書くとき、要素ごとに実装・要望メモへの参照があるかを1つずつ確認せず、周囲の要素の説得力に引きずられて未検証の要素を混ぜてしまう。低忠実度のモックでも「なぜこの要素があるか」を要素単位で言えるようにする(全体の裏付けチェックでは粒度が粗すぎて見逃す) | 済: 該当ブロックを削除。他の同様のlayout要素(冒険記サマリ等)は再確認済み(既存chronicleデータ・screen descriptionの記述に基づき残置)。**追記(同日)**: 同じscreens.yamlのD4作成時点でも同型の見落としが見つかった。依頼選択画面のactorsを`[player, gm]`としていたが、StartSessionは呼んだ本人がGmとして登録される仕様(domain-model.md)でusecases.yamlのstart-sessionもactors:[gm]のみであり、既存のRDRA参照元(usecases.yaml)と突き合わせずに感覚でactorsを書いていた。2件とも同一ファイルの別フィールド(layoutとactors)で発生しており、根本原因は「screens.yamlの各フィールドを書く際、既存のusecases.yaml等の一次データと突き合わせず単独で判断する」こと。次に同種のRDRA要素(特にactors/usecases参照)を書くときは、参照先ファイルの該当エントリを実際に開いて突き合わせる
- 2026-08-02 | 進め方見直し(post-p3.5-replanning-decisions.md)のQ1反映で、frozen語彙追加に伴うtools/docs-siteのソースコード修正(progress.ts・ProgressView.tsx・styles.css)へ、タスク(チケット)を発行せずその場で着手した。議論・文書整理タスクの範囲は文書反映までであり、コード修正は別タスクに切り出して人間判断を仰ぐべきだった。ユーザー指摘で中断し、変更はブランチdocs-site-frozen-statusへ退避。傾向:「このままではビルドが壊れる」という技術的必然を理由に、スコープ外のコード修正を同一作業へ畳み込みやすい(宣言範囲外の不具合を別チケット化する2026-08-02ルールと同根。範囲の境界が「フェーズの依存宣言」から「タスクの種別(議論/文書 vs コード)」に変わっただけで同じ構図) | 済: agent-operations.mdに「ソースコード変更はタスク発行が前提」を明文化
- 2026-08-11 | docs-site D6(要求requirements.yaml→業務フローbusiness-flow.yaml→
  画面screens.yamlの機械的導出)で、「荒い段階で確認を挟む」規律
  (post-p3.5-session-retrospective.mdの申し送り、rdra/README.md「更新の規律」)を
  適用したが、名前の列挙段階で確認した後も3回、ユーザーとの間で概念の括り方の
  ズレが発覚した: (1)作者向け4要求を「シナリオ執筆の流れ」1本にまとめて提示
  したが、執筆(1人で完結)と他作者との関わり(閲覧・派生)は動機が違うとの
  指摘で2本に分割、(2)「同じロケーションを使ってシェアワールド」を作者間
  共有フローの1ステップとして提示したが、実際はルールブック/ワールド設定
  という別文脈だった、(3)「ルールブック」を既存docsで足りる既存機能と
  自己判断し新規モデル化不要とみなしたが、実際は「読むと使える機能が広がる」
  成長体験の入口として業務フローが必要だった。傾向: 名前だけの列挙段階で
  確認する規律は「作り込む前に止まる」ことには機能したが、列挙した名前自体が
  複数概念を暗黙に束ねている(「・」区切りの複合名、抽象度の高い1語)場合、
  その束ね方が妥当かまでは検証対象になっていなかった。今回は列挙直後の
  会話で都度解けたため実害は無かった | 済: 3件とも会話中に解消しrequirements.yaml
  /business-flow.yaml/screens.yamlへ正しく反映。rdra/README.md「更新の規律」
  自体の文言改訂(「列挙した名前が単一概念か自問する」等の追記)は未実施
  (未反映。次にRDRA要素を列挙して確認を取る機会があれば、複合名・抽象名は
  下位項目まで分解して提示する運用を試す)
- 2026-08-02 | P3.5全4サイクルを通じて、crates/tabifuda-core(Character.owned_cards追加・Event::RewardsGranted/CardsDiscarded追加・LintIssue::PortableCardIsScenarioDependent追加)を変更したにもかかわらず、`crates/tabifuda-wasm/bindings/`(ts-rs生成のTSバインディング。gitコミット対象)を一度も再生成しなかった。task.mdの「P3.5は依存がcrates/のみでP3・Webとは独立」という記述自体は正しく、それを信頼してWeb側を確認しなかったこと自体は通常の運用(この判断はユーザーも当時気づいておらず、エージェント固有の誤りではない)。問題は「宣言された依存範囲の外で不具合が見つかった場合にどう扱うか」という運用ルールがこれまで存在しなかったこと。ユーザーから「apps/web側のEvent描画がRewardsGranted/CardsDiscardedに対応しているか確認したい」と指摘されて`pnpm -r typecheck`を実際に走らせて初めて発覚し、`packages/ui`の`eventRenderers.tsx`(HandlerMap網羅性チェック)がコンパイルエラーになった。さらに`tools/docs-site`(未分類テストスイート`party::tests`/`save::tests`、Character型のサンプルデータ不足)、`apps/web`(soloParty.tsのCharacter組み立て不足)でも同型の「新フィールド・新テスト追加箇所への追従漏れ」が芋づる式に見つかった。傾向: 「このフェーズはXに依存しない」という宣言は「Xに影響しない」ことを保証しない(Event/CharacterはRustとTSの両方から参照される共有型であり、Rust側の変更は生成物・手書きサンプルデータ双方に波及しうる) | 済: 今回分はbindings再生成・eventRenderers.tsx/gen-test-report.mjs/componentCatalogData.ts/soloParty.tsを修正し`pnpm -r typecheck`・`lint`・`build`全通過を確認。再発防止としてphase-cycleスキル「終わり方」にts型チェック手順を追加し、加えてagent-operations.mdに「宣言された依存範囲の外で見つかった不具合の扱い」節を新設(範囲外の発見は現フェーズに畳み込まず、docs/tasks/plans/へ別途書き出して人間判断を仰ぐ運用に変更)
- 2026-08-12 | P6 C1(カードコンポーネント新設)で、task.md/ui-visual-design.mdの「CardWirthの74x94相当が収まるサイズ」という具体的な数値記述を読みつつ、実装時にはアイコン領域を74x74の正方形にしてしまい「94」(縦横比)を反映し忘れた。傾向: 設計文書の具体的な寸法・数値記述は、読んだ時点では認識していても実装の細部(CSS値)を書く段階で丸めてしまいやすい。数値のある記述は実装直後に文書と1対1で見比べる一手間が要る | 済: design-syncで発覚し同PR内で`aspect-ratio: 74/94`に修正
- 2026-08-12 | P6 C1(TS/UIのみの変更、crates/は無変更)の終わり方で、phase-cycleスキル「終わり方」手順1が無条件に`cargo test/clippy/fmt`を指示していたため、差分ゼロのRustワークスペースに対して`cargo fmt --all -- --check`を意味なく実行した。さらに`pnpm --filter docs-site lint`をpackage.jsonのscriptsを確認せずに実行し、docs-siteに`lint`スクリプトが無く失敗した。傾向: 「終わり方」手順がコア(Rust)基準で書かれており、TS側のみの変更サイクルに対する条件分岐が無かったため、変更範囲を見ずに定型手順を字面どおり実行した。ユーザー指摘で発覚(「儀式的に意味のないcargoを行っている」「lintのないdocs-siteでlintを行おうとしている」) | 済: phase-cycleスキル「終わり方」手順1を`git diff --stat`で変更範囲を見て分岐する形に書き換え、pnpm系コマンドは`--if-present`を使う規約に統一(事前のpackage.json確認を不要にする)。あわせて「コア/フロントエンドで考慮点が違う」という指摘を受け、core-invariantsと対になる`client-conventions`スキルを新設し、CLAUDE.md「必読文書」表・コマンド節にもTS側の参照先を追記
- 2026-09-11 | CLAUDE.md「Rust規約」を.claude/rules/へ移転する際、対象パスを`crates/tabifuda-core/src/**`に絞る案を推奨した。しかし規約の1項目「コードコメントからdocs/tasks/を参照しない」は元々crates/全体(cli・wasm含む)に効いており、design-syncスキル手順6・docs/README.md・adr/0007がいずれも「crates/のコードコメント」と書いている。絞った結果cli/wasmでこの規約が抜けた。傾向: 規約を別ファイルへ移すとき「どこに置くか」(ファイル・パス)に意識が寄り、「その規約は今どの範囲に効いているか」(既存の参照元・適用範囲)を先に確認しなかった。移転は「参照元の洗い出し→適用範囲の確定→置き場所」の順で行うべきだった | 済: 翌ターンのセルフレビューで発覚し`paths: crates/*/src/**`へ広げ、design-syncの参照先を更新
- 2026-09-11 | P6 C2の終わり方で`pnpm --filter <pkg> lint --if-present`(CLAUDE.md・phase-cycleスキル・client-conventionsスキルに記載の構文)を実行したところ、lintスクリプト未定義のパッケージに対して`ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT`でexit 1になった。「lintスクリプトが無いパッケージでも失敗しない」という説明文と逆の挙動。pnpmは`run <script> [args...]`の形式で、script名より後の引数はscript自身への引数として渡され、pnpm自身のオプションとしては解釈されない。`--if-present`は`run`の直後(script名の前)に置く必要があった(正: `pnpm --filter <pkg> run --if-present lint`)。docs/tasks/tools/docs-site/plans/crosscutting-progress-view.mdに同根の副産物記録が既にあり、過去に一度発覚していたが横展開されていなかった。傾向: 「動くはずのコマンド」をドキュメントに書き写す際、実際に(特に失敗させる条件で)実行して確認しないと、字面が自然な誤りは長期間気づかれない。1箇所での発覚を他の参照箇所(grepで全文検索)に横展開する習慣が要る | 済: CLAUDE.md・phase-cycleスキル・client-conventionsスキルの3箇所を`run --if-present lint`に修正。修正後の構文をlintスクリプト未定義パッケージ(`@tabifuda/eslint-config`)で実際にexit 0になることを確認
- 2026-09-11 | PR作成前の検証で`cargo test --workspace`が全通過したためテスト完了と報告したが、直後にCIパイプラインで`crates/tabifuda-wasm/tests/boundary_roundtrip.rs`(wasm32ターゲット専用、`wasm-pack test --node`で実行)がビルドエラーになった。原因は`Character`に`owned_cards`フィールドが追加された際(このブランチより前のコミット、P3.5相当)、wasm境界テストのfixtureへの追従が漏れていたこと。`cargo test --workspace`はホストターゲットのみを対象にしており、`#![cfg(target_arch = "wasm32")]`なテストは最初からコンパイル対象に入らないため、ローカルの検証コマンド一式では検出できなかった。傾向: P3.5の教訓(ts-rs bindings再生成漏れ)と同根で、「crates/tabifuda-coreの型変更はwasm境界の成果物・テストに波及しうる」という注意点がbindings再生成には反映済みだったが、wasm-bindgen-testのfixtureには反映されていなかった | 済: fixture_party()に`owned_cards: vec![]`を追加し`wasm-pack test --node`で5件通過を確認。恒久対策として、phase-cycleスキル「終わり方」手順2に「公開型のフィールド変更時は`wasm-pack test --node`も実行する」を追記
- 2026-09-11 | P6 C1(カードコンポーネント新設)が、prompt-sample.md「1. チェックリストの作成(インタビュー式)」の対話プロセス(着手前に技術面・エッジケース・トレードオフを人間に質問してからチェックリスト化する)を経ずに実装されていたことに、C2着手前の会話でユーザー指摘を受けて気づいた。傾向: phase-cycleスキル「2. 実装」は「着手前にチェックリスト化する」とは明記するが「着手前に人間へ質問する」とは明記しておらず、質問を挟むかどうかがprompt-sample.mdの依頼文(非規範・ユーザーの選択任意)の利用有無に委ねられたままになっている。エージェント側が自発的に「このサイクルは仕様判断を伴うので質問すべきか」を判断する仕組みが無かった | 済: 事後的に4点(API拡張方針・配色先取りの扱い・命名規則明文化・a11y/テストの扱い)をユーザーに質問し、c1-checklist.md「事後確認」節に記録(命名規則は既にclient-conventions.mdへ反映済みと確認、他3点は現状維持/C2着手時に再検討で決着)。phase-cycleスキル「2. 実装」に「着手前に人間へ質問する」手順を追加(ユーザー承認済み、2026-09-11)
- 2026-09-11 | 同C2の着手前インタビューで「アイコンのaria-hidden化・Card自体の単体テスト不在」というa11y/テストの扱いをまとめて一括で質問し「現状のまま許容」の回答を得たが、これは「静的な表示のa11y」の話であり、実装完了後にedge-case-reviewerが指摘した「モーダルのフォーカストラップ未実装によりTabで背後のフォームへ移動でき、送信すると入力中の自由入力が無警告で消える」という**操作フロー起因のデータロス**は、質問時点では想定していなかった別種の問題だった。傾向: 「a11y」を一括りの論点として質問すると、「静的な読み上げ対応」と「動的な操作フロー(フォーカス移動・キーボードトラップ)がもたらす実害」という性質の異なる複数の懸念が1つの回答で片付いたように扱われてしまう。モーダル・ダイアログ等の新規UIパターンを導入するサイクルでは、着手前の質問に「フォーカストラップの要否」を独立した論点として明示的に含めるべきだった | 済: edge-case-reviewerの指摘で発覚し同PRで修正(最小のfocus trap実装)。次にモーダル系UIを追加するサイクルでは、a11y論点を「表示(読み上げ)」と「操作(フォーカス移動)」に分けて質問する
