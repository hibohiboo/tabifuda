# Phase 2 ふりかえり(2026-07-19)

対象: docs/tasks/projects/phase2/task.md C1〜C4(コミット `6045dff`〜`6183662`)。
位置づけ: 記録文書(非規範)。ここで挙げた対応項目は反映先の文書
(future-requirements.md / projects/phase3/task.md 等)が正であり、本文書は経緯を残す。

## 成果

コミット8件・29ファイル・約2,380行の追加で、Phase 2の完了条件
(単純討伐を最初から最後まで遊べる/lint・全テスト通過/fixtureがCIで
検証されている)をすべて満たした。

| サイクル | 成果物 |
|---|---|
| C1 | シナリオlint(`tabifuda-core::lint`、Error/Warning 8種)+`tabifuda-cli lint`+`shared/scenarios/`配置規約 |
| C2 | テンプレシナリオ「単純討伐」(lint 0件通過、既存Effectのみで表現) |
| C3 | CLIプレイループ(提案→GM裁定含む)+運用ログ層(種別+IDのみ、本文漏れをテストで固定) |
| C4 | 通しプレイfixture+リプレイ・スナップショットテスト+冒険記描画+CLIスモーク2本 |
| 追加 | 作者データへのBoundedString retrofit / PO向けデモ手順(docs/demo.md) |

テストは開始時140件→終了時153件。decide/applyのルール本体には
BoundedString適用以外の変更を入れずにCLI層を積み上げられた。
「ロジックはコアにしか置かない」というPhase 1の設計が機能した証拠。

## うまくいったこと

- **開始の儀式が手戻りを防いだ。** C1着手前に「lintの置き場所・探索範囲・
  重大度・配置場所」の4つの未確定点を洗い出して確認してから実装したため、
  実装中の手戻りがゼロだった。過去ジャーナル(P1 C1〜C5)の教訓が効いた
- **fixtureの3役設計が低コストで成立。** 1つのイベント列JSONが回帰検知・
  シリアライズ互換・CLIスモークの生成元を兼ねる構成を追加の仕組みなしで実現
- **手動での通しプレイ検証が2つの問題を捕まえた。** 自由入力スキップ時の
  カード中断バグ(修正済み)と、`AdvancePhase`二重踏み時の`NoNextPhase`拒否
  (正しい挙動の確認)を、テストではなく実際に遊ぶことで発見した
- **ログの二重規律(冒険記=本文OK/運用ログ=種別+IDのみ)を最初から
  テストで固定**し、実プロセス越しの結合テストでも漏れがないことを検証した

## 課題(ジャーナル記録済み2件)

1. **cross-cutting.mdの読み漏れ(最重要)。** C1/C2で作者データへの
   BoundedString適用(cross-cutting.mdがP2実施と明記)を見落とし、C3開始時に
   発覚→retrofitコミット(`24c587c`)で修正。原因は「シナリオ形式・データ作成」
   という切り口で着手したため、それが「UGC(自由入力)を含む横断的関心事」だと
   判断しなかったこと。教訓: **名前・説明文・narrationのような自由入力を
   含みうるフィールドを新設するタスクは、横断文書(cross-cutting.md)も必読に
   含める**(agent-journal.md 2026-07-19に記録)
2. **`Option<Option<T>>`の意味の取り違え。** 自由入力スキップの実装で、
   スキップを「カード使用のキャンセル」と誤実装。手動プレイで発見・修正済みだが、
   この経路(スキップしてもカードは出る)は自動テスト未固定
   (agent-journal.md 2026-07-19に記録)

## 気づきと対応(反映先)

| 気づき | 対応 | 反映先 |
|---|---|---|
| C2の停止ポイント(データレビュー)がほぼ素通しになった。確認観点の提示のみで即コミット指示だった | シナリオデータの人間レビューは実プレイ(docs/demo.md の手順)で行う運用に変更 | projects/phase3/task.md「P2からの申し送り」 |
| 使用済みカードが手札に残り続ける仕様は、POが最初に気づく違和感になりうる。除去は`CardInstanceId`発番の再検討(engine.rsのコメントが予告済み)を伴う | 将来要望として整理し、着手時期は別途判断 | future-requirements.md §6 |
| lintのWarning(到達不能・詰み)を実際に出すシナリオが未検証。検出ロジックはテスト済みだが作者体験は見ていない | シナリオ追加サイクルでの確認観点として申し送り | projects/phase3/task.md「P2からの申し送り」 |
| フェーズ完了時のふりかえり自体が指示待ちだった | フェーズ最終サイクル完了時のふりかえりを手順化 | agent-operations.md「フェーズ完了時のふりかえり」+ phase-cycleスキル |
