# 将来要望メモ

**位置づけ: 将来要望専用**。今は作らないが、設計判断時に壊さないよう
意識しておく要望集。各項目に「現行モデルへの影響」を付記する。

**「現在の要件」(いま作ると決まっているもの)の置き場ではない。**
現在のスコープの正は design/ の規範文書(domain-model.md、特に
「ソロMVPでの簡略化」)とフェーズタスク(tasks/、全体像は roadmap.md)。
要望が採用され実装に着手したら、仕様を規範文書へ反映し、本メモから該当節を
削除する(経緯は git 履歴と domain-model.md「決定の経緯(参照)」で追う。
実例: 旧§6 カードの消費・除去)。

## 1. セッションの中断と並行プレイ

**中断・再開(単一セッション)は実装済み**: [tasks/projects/phase3.5/task.md](../tasks/projects/phase3.5/task.md)
C1で採用(2026-08-01)。設計はdomain-model.md「セッションの保存と再開
(CLIの決定)」節、パーティのスナップショット方式は同文書「セッション状態」節の
`Session.party`定義を参照(将来要望としてのこの節の役目は終えた)。

複数セッションファイルによる並行プレイが自然に成立することはC4で
結合テストにより確認済み(2026-08-02。tabifuda-cli/tests/play_cli.rs
「同じシナリオから開始した複数セッションは保存ファイルが衝突せず独立して
再開できる」)。同一キャラの並行参加制約(下記の派生論点)は本メモに残る。

派生論点(未決):
- 同一パーティの並行参加を許すと、報酬の書き戻しが競合しうる。
  「同一キャラは同時に1セッションまで」等の制約を入れるか、
  終了順に単純マージするか。
- **セッション募集(非同期マルチプレイ、未モデル化)**: 非同期マルチプレイ
  (P4)では、GMが複数シナリオをストック・カスタマイズし、そのうち1つを
  使ってセッションを募集し、プレイヤーが集まってから実際にプレイが始まる
  流れが想定される(2026-08-02、docs-site D4画面レビューで発見)。
  現行モデルの`StartSession`はパーティが確定済みであることを前提に
  呼ばれ、呼んだ本人が即座にGmとして登録される(domain-model.md
  「セッション開始」)ため、「募集中(参加者未確定)」という中間状態を
  表現できない。P4着手時、募集用の新Command(仮称)またはStartSessionの
  事前段階として検討が必要。GMが複数セッションを並行管理する前提も
  合わせて確認する(1で確認済みの複数セッションファイル自体は成立するが、
  「1人のGMが複数の進行中セッションを持つ」UI/権限面は未検討)。

## 2. キャンペーンシナリオ/セッション

要望: 前のセッションを攻略したパーティのみ参加できるセッション。

設計方針(案):
- シナリオmetaに `prerequisites: Vec<Requirement>` を追加
  (例: `ClearedScenario(ScenarioId)`)
- パーティ/キャラクターのマスターデータに攻略履歴
  `cleared: Vec<(ScenarioId, Outcome, SessionId)>` を持たせる
- セッション開催時(StartSession)の検証で判定

現行モデルへの影響: なし(マスターデータ層とシナリオmetaの拡張のみ)。
攻略履歴は§3のセッション終了処理で記録するのが自然。

## 3. 報酬とセッション終了処理

**手札からの持ち出し(portable)とパーティファイルへの書き戻しは実装済み**:
[tasks/projects/phase3.5/task.md](../tasks/projects/phase3.5/task.md)
C3(2026-08-02)・C4(2026-08-02)で採用。既定不可・`#portable`タグで明示可・
Marker常に不可・持ち帰りはCardDefの凍結コピー・portableカードはシナリオ
非依存に制約(C3)、`--party`ファイルへの書き戻し(C4)を、domain-model.md
「持ち出し可否(portable)」「セッション終了処理(finalize)」「パーティ
ファイル」節へ移した。

残る要望(本節の対象):
- **シナリオ作者が明示的に定義する報酬**: `rewards: Vec<Reward>`
  (`GrantCard(CardId)`, `GrantPoints(u32)` など。Outcome別に定義できると
  よい)。手札に無いカードでも「クリア報酬」として付与できる仕組み。
  C3は「セッション中に手に入れていたportableなカードを持ち帰る」経路のみ
  実装しており、この「シナリオが明示的に授与する」経路は未着手
- **キャラメイク用ポイントの付与**(`GrantPoints`): 現行モデルには
  ポイントを消費するキャラメイクの仕組み自体が無いため、置き場が定まって
  いない。キャラメイク機構の設計とあわせて検討する
- 攻略履歴(§2)と連動したマスターデータへの書き戻し

現行モデルへの影響: 小〜中。`rewards: Vec<Reward>`はScenarioへの新規
フィールド追加(`#[serde(default)]`で後方互換)。`GrantPoints`は
キャラメイク機構が無い限り宙に浮くため、先に置き場を決める必要がある。

## 4. カードのタグシステム

要望: シナリオがカードのタグを見て処理できる。
例: 蜘蛛の巣で塞がれた道は、`#炎` タグを持つ任意のカードで焼き払える。

設計方針(案):
- `CardDef.tags: Vec<Tag>` を追加(Tagは文字列ベースの新型)
- Conditionに `HasCardWithTag(Tag)` を追加
- シーン側に「タグ受け」の仕組み: シーンやオブジェクトカードが
  `accepts: [{ tag: 炎, effects: [...] }]` を持ち、該当タグのカードを
  出すとそのeffectsが発動する(カード自身のeffectsとは別系統)
- 汎用性の核: カード作者は用途を知らずにタグを付け、シナリオ作者は
  カードを知らずにタグで受ける。疎結合になる

現行モデルへの影響: **v0.1の時点で `tags` フィールドだけ予約しておく価値あり**
(空でよい)。シリアライズ形式に後から足すより、最初からある方が
シナリオデータの互換性管理が楽。

## 5. 条件付き選択肢とテキスト変化

要望:
- 特定カード所持、シナリオ経験、クリアフラグで選択肢が増える
- キャラクター属性(性別など)でNPCの会話内容が変わる
- シーン分岐まではいかない「会話が少し変わるだけ」も表現したい

設計方針(案):
- 選択肢の条件: v0.1の `CardDef.requires: Vec<Condition>` がそのまま土台。
  Conditionを拡張する:
  - `ClearedScenario(ScenarioId)` (§2と共通)
  - `CharacterAttr(AttrId, AttrValue)` (性別等。Characterに属性mapを追加)
  - 「条件を満たさないカードを 非表示にする/グレーアウトで見せる」は
    カード側で選べるとよい(見えるが選べない、は誘い文句として有効)
- テキスト変化: narrationや台詞を条件付きフラグメントの列にする
  ```
  narration: [
    { text: "店主が顔を上げた。" },
    { text: "「おや、また来たのかい」", if: ClearedScenario(prev) },
    { text: "「いらっしゃい、初めての顔だね」", unless: ClearedScenario(prev) },
  ]
  ```
  分岐(別シーン)を作るほどでもない差分を安価に表現できる

現行モデルへの影響: 中。narrationを `String` から
`Vec<ConditionalText>` に変える判断は早めが楽(v1入り口で検討)。
Conditionのenum拡張は互換性を壊さない。

## 6. フォーク公開とシナリオ作者への還流

要望:
- GMがセッション中に改編(パッチ)したシナリオを、次のセッションでも使いたい
- シナリオ作者は、GMたちの改編を差分としてレビューし、元シナリオに取り込みたい

実装済みの範囲(v0.2、CLIフォーク出力):
- セッション終了後、パッチ適用済みスナップショットを別シナリオとして保存できる
  (`meta.forked_from`で由来を追跡。domain-model.md「フォーク出力」参照)

残る要望(本節の対象):
- **パッチ列形式の永続化**: 「元シナリオ+パッチ列」をそのまま保存・公開する
  フォーマット。スナップショットと違い、元シナリオの更新への追従や
  差分レビューが可能になる
- **作者への還流ワークフロー**: フォークの差分(パッチ列)を作者が
  レビューして本家へ取り込む流れ(Pull Request相当)。P4のサーバ側
  (共有・配布)とセットで検討
- 複数セッションのパッチの合成・衝突解決

現行モデルへの影響: 小。`ScenarioMeta.forked_from`と構造化パッチ
(`ScenarioPatch`)が既に土台。パッチ列の永続フォーマットと、
`validate`を「セッション状態なし(シナリオ単体)」でも使える形に
整理する程度。

## 7. Web版のシナリオフォーク保存

要望: CLI版(`tabifuda-cli play`)にある「セッション終了後、パッチ適用済み
シナリオをフォークとして保存する」機能(domain-model.md「フォーク出力」)を
Web版(apps/web)でも使えるようにしたい。

現状: P3 C3時点でWeb版はCLIとほぼ同等の操作(提案・GM裁定・自由入力・
タイムライン)を実装したが、フォーク保存は対象外のまま据え置く
(2026-08-01決定)。**採用予定のフェーズではない**(P3.5は「CLI永続化」
専用でWebを対象としないため、そのままでは採用先にならない)。
[tasks/projects/phase3.5/task.md](../tasks/projects/phase3.5/task.md) C1
(セッション保存形式の設計)着手時に、同じ保存思想をWeb版フォーク保存にも
応用できないか**見直すチェックポイント**とする(2026-08-01決定。実装を
約束するものではない)。

設計方針(案・未検討): ブラウザはCLIのようにファイルシステムへ自由に
書き込めないため、File System Access API(対応ブラウザのみ)かダウンロード
(Blob経由)での保存になる見込み。P3.5(CLI永続化)の実装状況・セーブ
フォーマットとの関係も着手時に確認する。

現行モデルへの影響: なし(CLIの`build_fork`相当のロジックをTS側に持つか、
wasm境界に新規関数を追加するかの検討が必要になる程度)。

## 8. 段階的な成長体験(ユーザーとして「できること」が増えていく)

要望: プレイヤーキャラクターやパーティの成長だけでなく、ユーザー自身が
触れられる範囲(選べるタグ、シナリオ作成時にできること等)も、プレイ・
執筆を重ねるごとに広がっていく実感がほしい(2026-08-11、D6の要求整理中に
ユーザーから提示)。例:
- ロケーション・世界観設定を読んだ後に作るキャラクターは、そのタグを
  キャラメイクの選択肢として使えるようになる
- シナリオ作成時に使える機能が、作者の実績に応じて段階的に増えていく

位置づけ: 個別機能というより、§4(タグシステム)・§5(条件付き選択肢と
テキスト変化)・§2(キャンペーンシナリオ)の**上位にある設計原則**。
「小さく始めて機能を増やしていく」というプロジェクトの進め方そのものを、
ユーザー体験としても反映したいという方向性であり、今は個別のCommand/Event
提案を伴わない。

設計方針(案・未検討):
- タグシステム(§4)が実装されれば、「読んだロケーションのタグをキャラメイク
  の選択肢に追加できる」は自然な応用先になる
- 段階開放の判定はConditionの拡張(§5のClearedScenario等)と同じ土台で
  表現できる可能性がある
- 作者側の「できることが増える」は、現状は基本の執筆要求(シナリオを作る
  こと自体)すら requirements.yaml に無い状態(D6で整備中)。まず基本機能を
  揃えてから、その上に積む話として扱う

現行モデルへの影響: なし(今は方向性のメモ。個別機能が具体化した時点で
本節を分割し、該当する既存節(§4・§5等)または新節へ実装対象として移す)。

**具体化(2026-08-11追記)**: 「ルールブック(システム提供の固定コンテンツ)を
読み進めると、シナリオ作成で使える機能・キャラメイクで選べる要素が広がる」
という形で、本節の考え方をrequirements.yamlの要求
(`unlock-authoring-by-rules`、`unlock-character-options-by-rules`)・
business-flow.yamlの業務フロー(ルールを読んで使える機能が広がる流れ)へ
具体化した。ルールブック自体の内容(何が書かれているか)はまだ未検討。

## 9. シナリオ作者向けの作成・編集手段

要望(2026-08-11、D6の要求整理で発見): 作者がシナリオ(phases > scenes の
木構造、カード定義)を作る・直すための専用の手段(GUIエディタ、最低限でも
書式ガイド)がほしい。

現状: シナリオはJSON/RustコードとしてリポジトリのソースからCLIやWebが
読み込む形で手書きされている(simpleHuntテンプレート等)。作者向けの
専用画面・ドキュメントは無く、「作者としてシナリオを作りたい」という
要求自体がこれまで requirements.yaml に存在しなかった([../rdra/screens.yaml](../rdra/screens.yaml)
の author-page で2026-08-02に指摘済みのギャップを、要求レイヤーまで
遡って整理したもの)。

設計方針(案・未検討): P3範囲の作者向けGUI(author-page画面、
[../rdra/screens.yaml](../rdra/screens.yaml)参照)を想定。データ形式自体
(phases > scenes、CardDef)は既存のまま、それを編集するUIを追加する話
であり、コアモデルへの影響は薄い。

現行モデルへの影響: 小(UI追加のみ。データ形式は変えない見込み)。

## 10. シナリオ作者間の共有・派生

要望(2026-08-11、D6の要求整理でユーザーから提示):
- 他の作者が書いたシナリオを閲覧したい
- 他シナリオをベースにコピーし、自分のシナリオ作りを始めたい
- 同じロケーション(舞台設定)を複数シナリオで共有し、シェアワールドを
  表現したい

現状: シナリオはローカルのJSON/Rustファイルとして単独で存在し、作者間で
閲覧・共有する仕組みが無い。§6(フォーク公開とシナリオ作者への還流)は
GMがセッション中に改編した結果を作者へ還流する話で、本節の「他作者の
シナリオを読む・ベースにする」という執筆前の共有とは動機が異なる
(ただし`ScenarioMeta.forked_from`によるコピー元追跡の仕組みは両者で
共用できる可能性がある)。

設計方針(案・未検討):
- 「閲覧」「コピーして開始」はP4のサーバ側(共有・配布)が前提になる見込み
  (現状はローカルファイルのため、公開・共有という概念自体が無い)
- 「シェアワールド」(共通ロケーション)は、ロケーション情報をシナリオ間で
  参照する仕組みが必要。現行モデルにロケーション/世界観設定の情報モデルが
  無いため、まずその設計が要る

現行モデルへの影響: 大。P4(凍結中)のサーバ側設計と、ロケーション情報
モデルの新設が前提になる。

## 11. 依頼(シナリオ)選択を張り紙カードから選ぶUI

要望(P3.5完了後の見直し、Q2の演出検討時に案として出た。
[tasks/plans/post-p3.5-replanning-decisions.md](../tasks/plans/post-p3.5-replanning-decisions.md)
Q2): 依頼(遊べるシナリオ)の選択を、CardWirthの「張り紙」のようなカードUIで
行いたい。

現状: シナリオ選択機能自体が未実装(apps/webは`simple-hunt`固定で起動する)。
[design/ui-visual-design.md](../design/ui-visual-design.md)「カードの
ビジュアル方針」で決定したカード自体の見た目(白銀比・既定アイコン・
2サイズ)は[tasks/projects/phase6/task.md](../tasks/projects/phase6/task.md)
で実装するが、依頼選択画面(複数シナリオからの選択UI)はシナリオ選択機能の
新規実装を要するため P6 のスコープ外とした(2026-08-12)。

現行モデルへの影響: 小(表示層の話。シナリオ選択機能自体は§1の
「セッション募集」との関連が強い)。

## v0.1への逆輸入候補(小さい予約)

1. `CardDef.tags: Vec<Tag>` を空フィールドとして今から持つ(§4)
2. `Session.party` はマスターの参照でなくコピー、と設計文書に明記(§1)
3. Condition/Effect のenumは追加前提として扱う(シリアライズに種別名を含める)
