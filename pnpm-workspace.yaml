# ワークスペース構成(パッケージマネージャの選定根拠: docs/adr/0002-package-manager.md)。
# packages/ui・packages/eslint-configは前倒し追加(packages/schemaはP4のまま)
packages:
  - "tools/*"
  - "apps/*"
  - "packages/*"
allowBuilds:
  esbuild: true
