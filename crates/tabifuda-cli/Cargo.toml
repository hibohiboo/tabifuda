[package]
name = "tabifuda-cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "tabifuda-cli"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tabifuda-core = { path = "../tabifuda-core" }
