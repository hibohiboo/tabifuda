[package]
name = "tabifuda-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
ts-rs = { version = "12.0.1", optional = true }

[dev-dependencies]
proptest = "1"
proptest-derive = "0.5"

[features]
ts = ["dep:ts-rs"]
